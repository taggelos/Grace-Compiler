fun hello () : nothing
var i : int;
fun swap (x, y : int) : nothing
var j : int;
var ch : char[];
var z : int[][2];
{
	if 3 < 2 then
		j <- 0;
	i <- 1;
	j <- 1;
	$ch <- "meme"[0];
}
{
	
  i <- i + 2;
  $puts("Hello world!\n");
  swap(1,2);
}
