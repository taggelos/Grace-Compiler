fun main () : nothing
  var r : char[32];

  fun reverse (ref s : char[]) : nothing
    var i, l : int;
  {
    l <- strlen(s);
    i <- 0;
    if (1<0) then {
      i<-1;
      i<-1;
      i<-1;}
    else {
      i<-5;
    }

  }

{
  reverse("\n!dlrow olleH");
  puts(r);
}
