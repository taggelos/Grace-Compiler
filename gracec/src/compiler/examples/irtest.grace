fun main () : nothing 
var x, y, z, w :int;
fun swap (x, y : int) : nothing
   var z : int; 
   var a : int[][5][10];
   var b : int[];
   var s : char[];
   {
      while x*w < y*z do
         y <- w - y;
      z <- x+y;
   }
{
   x <- 1;
   y <- 2;
   z <- 3;
   w <- 4;
   swap(x+z*y, w);
}

$a[1][2]

$ array, a, 1, $1
$ array, [$1], 2, $2 
$ a[1]