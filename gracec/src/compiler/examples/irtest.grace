fun main () : nothing 
fun swap (ref x, y : int[]) : int
   var  i : int; 
   
   {
      i<-i+1+2+x[i];
      x[0]<-x[i];
      return x[i];
   }

         var t : int[];
      {
         swap(t,t);
      }

