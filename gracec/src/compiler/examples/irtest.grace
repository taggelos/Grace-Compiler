fun main () : nothing 
fun swap (x, y : int[]) : int
   var  i : int; 
   var a : int[][];
   var s : char[];
   {
      $i <- x[x[x[1]]];
      $a[1][i+1] <- a[1][2];
      $x[i]<-i+1+--2+x[i];
      i <- 1- -1;
      i <- strlen(s)-1;
      $if (0>1 or 0>2) and 0>3 then
      if (3>4 and 0>1) or 1>0 then
         return -1;
      $else   
      $return 2;
   }

         var t : int[];
      {
         swap(t,t);
      }

