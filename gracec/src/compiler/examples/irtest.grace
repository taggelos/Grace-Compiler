fun main (i: int) : int 
fun swap (x, y : int[]) : int
   var  i : int; 
   var a : int[][];
   
   {
      a[1][i+1] <- a[1][2];
      i<-i+1+2+x[i];
      if 0>1 or 0>2 and 0>3 then
         return 0;

      return 2;
   }

         var t : int[];
      {
         swap(t,t);
         return 1;
      }

